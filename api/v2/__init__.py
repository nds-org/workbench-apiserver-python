# The V2 API does not use a database, but instead
# stores all of its metadata within the Kubernetes
# cluster itself using CustomResourceDefinitions (CRDs).


# User accounts are created as TBD

# Catalog applications are created as a WorkbenchAppSpec

# User applications are created as a WorkbenchUserApp
